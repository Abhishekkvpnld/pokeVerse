import LoadingComponents from "@/app/components/LoadingComponent";

const Loading = () => {
    return (
      <div className="flex flex-col items-center justify-center h-screen bg-white">
        <LoadingComponents />
      </div>
    );
  };
  
  export default Loading;